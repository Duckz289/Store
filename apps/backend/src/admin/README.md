# Medusa Admin fallback

Thư mục này chỉ giữ cấu hình tối thiểu để Medusa Admin gốc hoạt động tại `/app`.

Dashboard vận hành chính nằm trong `apps/admin` và chạy tại `/admin`. Không thêm
route, widget, bản dịch hay presentation feature mới vào thư mục này.
